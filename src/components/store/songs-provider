import react from 'react';

const SongsProvider = react.createContext({
    songs= {
    trackList: [],
    heading:'Random Songs'}
},
 function getSongsResponse() {
    fetch(``).then(res => console.log(res)).catch(err => console.log(err))
});

export default SongsProvider;